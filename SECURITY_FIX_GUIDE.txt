══════════════════════════════════════════════════════════════════════════════
  GITHUB SECRET SCANNING FIX — Creative Metal Industries
  Date: 2026-06-17
══════════════════════════════════════════════════════════════════════════════

ISSUE:
  GitHub detected an exposed Google API Key in commit bdad7b0e.
  The key was inside browser_session/Default/Cache/Cache_Data/ files
  that were accidentally committed from Playwright's browser session.

══════════════════════════════════════════════════════════════════════════════
  SECRETS FOUND IN REPOSITORY
══════════════════════════════════════════════════════════════════════════════

  1. Google API Key (in browser cache)
     Location: browser_session/Default/Cache/Cache_Data/*
     Commit: bdad7b0e
     Risk: HIGH — can be used to make API calls on your account
     Action: Revoke key in Google Cloud Console + remove from history

  2. Admin Token (hardcoded fallback)
     Location: src/routes/api/reviews-admin.ts
     Value: Ss@9998280619 (was hardcoded as fallback)
     Commit: 7fd6306
     Risk: MEDIUM — allows deleting reviews
     Action: FIXED — removed hardcoded fallback, now requires env var

  3. SMTP App Password
     Location: .env (NOT committed — safe)
     Value: jomd cpnm haxd gnfu
     Risk: LOW — .env is in .gitignore, never committed
     Action: No action needed (but rotate password anyway)

══════════════════════════════════════════════════════════════════════════════
  FIXES ALREADY APPLIED (in code)
══════════════════════════════════════════════════════════════════════════════

  1. ✅ Removed hardcoded "Ss@9998280619" fallback from reviews-admin.ts
     Now: process.env.ADMIN_TOKEN (no fallback — fails if not set)

  2. ✅ Removed browser_session/ from git tracking
     Command: git rm -r --cached browser_session/

  3. ✅ Updated .gitignore to block:
     - browser_session/
     - **/Cache/
     - **/Cache_Data/
     - **/Default/
     - .env.* (except .env.example)

  4. ✅ Created .env.example (safe template with placeholder values)

══════════════════════════════════════════════════════════════════════════════
  STEP 1: REVOKE THE EXPOSED GOOGLE API KEY
══════════════════════════════════════════════════════════════════════════════

  1. Go to https://console.cloud.google.com/apis/credentials
  2. Find the key that starts with "AIza..."
  3. Click it → Delete or Regenerate
  4. If you don't use any Google API — just delete it
  5. This prevents anyone from using the exposed key

══════════════════════════════════════════════════════════════════════════════
  STEP 2: COMMIT CURRENT FIXES
══════════════════════════════════════════════════════════════════════════════

  Run these commands in your terminal:

  cd "/home/himanshus/Projects/creative metal industryies web page"
  git add .gitignore
  git add creative-metal-industries/.env.example
  git add creative-metal-industries/src/routes/api/reviews-admin.ts
  git commit -m "security: remove exposed secrets, add .env.example, fix hardcoded tokens"

══════════════════════════════════════════════════════════════════════════════
  STEP 3: REMOVE SECRETS FROM GIT HISTORY
══════════════════════════════════════════════════════════════════════════════

  Option A: Using git-filter-repo (RECOMMENDED)
  ──────────────────────────────────────────────

  Install git-filter-repo:
    pip install git-filter-repo

  Remove the entire browser_session directory from ALL history:
    git filter-repo --path browser_session/ --invert-paths --force

  This rewrites history to make it as if browser_session/ never existed.

  Option B: Using BFG Repo-Cleaner
  ─────────────────────────────────

  Download BFG: https://rtyley.github.io/bfg-repo-cleaner/

    java -jar bfg.jar --delete-folders "browser_session" .
    git reflog expire --expire=now --all
    git gc --prune=now --aggressive

══════════════════════════════════════════════════════════════════════════════
  STEP 4: FORCE PUSH TO GITHUB
══════════════════════════════════════════════════════════════════════════════

  After cleaning history:

    git push origin main --force

  ⚠️ WARNING: Force push rewrites remote history.
  All collaborators must re-clone the repository after this.

══════════════════════════════════════════════════════════════════════════════
  STEP 5: DISMISS GITHUB ALERT
══════════════════════════════════════════════════════════════════════════════

  1. Go to your repository on GitHub
  2. Security tab → Secret scanning alerts
  3. Find the Google API Key alert
  4. Click "Close as" → "Revoked" (if you deleted the key in Step 1)
  5. Done

══════════════════════════════════════════════════════════════════════════════
  STEP 6: ROTATE ALL PASSWORDS (RECOMMENDED)
══════════════════════════════════════════════════════════════════════════════

  Even though .env was never committed, it's good practice to rotate:

  1. Gmail App Password:
     → Google Account → Security → App Passwords → Delete old → Create new
     → Update .env with new password

  2. Admin Token:
     → Change ADMIN_TOKEN in .env to a new strong random string
     → Example: openssl rand -hex 16

══════════════════════════════════════════════════════════════════════════════
  COMPLETE COMMAND SEQUENCE (copy-paste)
══════════════════════════════════════════════════════════════════════════════

  # Step 1: Commit current fixes
  cd "/home/himanshus/Projects/creative metal industryies web page"
  git add -A
  git commit -m "security: remove browser_session, fix hardcoded tokens, add .env.example"

  # Step 2: Install git-filter-repo
  pip install git-filter-repo

  # Step 3: Remove browser_session from all history
  git filter-repo --path browser_session/ --invert-paths --force

  # Step 4: Force push
  git remote add origin https://github.com/salescreativemetalind/creative-metal-web-page.git
  git push origin main --force

  # Step 5: Revoke key at https://console.cloud.google.com/apis/credentials
  # Step 6: Dismiss alert on GitHub Security tab

══════════════════════════════════════════════════════════════════════════════
  FILES CHANGED SUMMARY
══════════════════════════════════════════════════════════════════════════════

  File                                          Change              Why
  ────────────────────────────────────────────  ──────────────────  ───────────────────
  .gitignore                                    Updated             Block browser_session, Cache, .env.*
  creative-metal-industries/.env.example        Created (new)       Template showing required env vars
  creative-metal-industries/src/routes/api/     Modified            Remove hardcoded token fallback
    reviews-admin.ts                                                 (was "Ss@9998280619")
  browser_session/                              Removed from git    Contains Google API key in cache

══════════════════════════════════════════════════════════════════════════════
  FRONTEND vs BACKEND KEY ANALYSIS
══════════════════════════════════════════════════════════════════════════════

  The exposed Google API key was NOT in your application code.
  It was in Playwright's browser cache (from the SEO rank checker).
  
  Your application has NO Google API keys — frontend or backend.
  No frontend changes needed.
  
  The only secrets your app uses are:
  - SMTP_USER / SMTP_PASS → backend only (server API route) ✅ safe
  - ADMIN_TOKEN → backend only (server API route) ✅ safe
  
  None are exposed to the client/frontend.

══════════════════════════════════════════════════════════════════════════════
  END
══════════════════════════════════════════════════════════════════════════════
