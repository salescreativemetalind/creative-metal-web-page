# =============================================================
# robots.txt — Creative Metal Industries
# https://www.creativemetalind.com
#
# Framework : SolidStart 1.0.11 + Vinxi 0.5.7 + Nitro 2.13.4
# Renderer  : SSR (server-side rendered, Vercel preset)
# Last updated: 2026-06-10
# =============================================================

# ── All crawlers ───────────────────────────────────────────────
User-agent: *

# Allow all public pages and assets (HTML, CSS, JS, images)
Allow: /

# Block only POST-only API handler — not a page, returns JSON
Disallow: /api/enquiry

# Block SolidStart internal data endpoint (JSON hydration, not a page)
Disallow: /__data.json

# NOTE: /_build/ /_server/ /_nitro/ /_vinxi/ are NOT blocked here.
# Semrush flagged 35 blocked resources because blocking those
# directory prefixes also blocks JS/CSS chunks served from them.
# Googlebot needs JS/CSS to render SSR pages correctly.
# Only the specific non-page endpoints above are disallowed.

# ── Sitemap ────────────────────────────────────────────────────
Sitemap: https://www.creativemetalind.com/sitemap.xml
